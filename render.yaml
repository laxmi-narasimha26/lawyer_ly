services:
  - type: web
    name: legal-ai-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/main_render.py
    envVars:
      - key: ENVIRONMENT
        value: production
    healthCheckPath: /health